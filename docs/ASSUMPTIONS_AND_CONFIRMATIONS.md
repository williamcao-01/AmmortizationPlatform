# 当前数据约束

- 运行时唯一输入为 `data/customer_snapshot/` 中两份 `20260812` Excel。
- 不加载历史 Excel、组织机构表、规则文档、旧 CSV 或历史 SQLite。
- 组织口径使用台账中的所属单位字段。
- 客户后续更新快照时，替换这两份文件并重启服务；系统不保留旧快照并存。
- SAP 预测结果由客户人工核验；系统提供逐资产、逐月和逐规则记录。
