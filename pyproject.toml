[project]
name = "asset-depreciation-ontology-poc"
version = "0.1.0"
description = "Sample POC for ontology-driven asset depreciation forecasting."
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
pattern = "test_*.py"
