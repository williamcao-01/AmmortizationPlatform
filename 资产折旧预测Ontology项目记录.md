# 资产折旧预测 Ontology 项目记录

本文档是资产折旧预测 ontology POC 的内部项目底稿，用于统一当前团队对业务、技术方案、实施路径和风险的理解。它不是客户宣传稿，也不是最终交付方案；它的价值在于把我们现在已经形成的判断沉淀下来，方便后续继续拆任务、做原型和与客户沟通。

## 1. 项目背景与目标

当前场景来自客户提供的财务场景 Excel，核心需求是“资产折旧预测”：基于资产折旧政策与资产变动信息，预测未来 5 年或任意期间的折旧明细和汇总结果，并服务年度预算编制。

这个项目不应被理解为“用大模型算折旧”，也不应被理解为“用 ontology 替代 SAP/ERP 固定资产模块”。更准确的定位是：

```text
以企业现有资产、折旧政策、预算假设和资产生命周期事件为基础，
构建资产折旧语义层，
通过 ontology + 规则校验 + 确定性计算 + LLM Harness，
实现折旧预测、异常识别、原因解释和假设分析。
```

业务目标包括：

- 预测当前资产和未来预计资产在未来期间的折旧明细。
- 按公司、部门、成本中心、资产类别、预算版本等维度形成汇总。
- 解释折旧费用变化的原因，例如新增、减少、减值、调拨、政策变化。
- 识别折旧码错配、缺少关键字段、使用年限异常、残值率异常等问题。
- 支持 What-if 假设分析，例如新增资产金额变化、转固日期延后、折旧年限调整。

技术目标包括：

- 构建企业资产折旧业务的语义层，而不是凭空设计抽象 ontology。
- 将客户现有 SAP 配置、制度文件、预算模板、历史报表和人员经验转化为可查询、可校验、可推理的知识结构。
- 将大模型限制在受控的解释、问答、抽取和工具编排位置，避免其承担确定性财务计算。

## 2. 当前业务理解

资产折旧的基础逻辑并不复杂。企业购买固定资产后，不会一次性把全部成本计入费用，而是在资产预计受益期间内按一定规则分摊。常见直线法可简化为：

```text
月折旧额 = (原值 - 残值 - 累计减值) / 可折旧月份
```

如果只看单个资产，并且原值、残值率、使用年限、开始日期、折旧方法都已知，折旧预测并不难。真实企业场景的复杂性不在公式本身，而在公式之前和公式之后。

资产折旧预测至少涉及四类对象：

- 当前资产：已经在 SAP/ERP 固定资产模块中存在的资产卡片。
- 未来资产：预算或投资计划中预计新增、转固但尚未形成正式资产编号的资产。
- 资产事件：新增、转固、减少、报废、减值、调拨、停用、改良等生命周期事件。
- 预算假设：未来新增金额、转固日期、减少日期、减值金额、预算版本和组织归属。

因此，预测逻辑不是简单地从固定资产模块拉一张表。真实链路往往跨多个系统：

- SAP 固定资产模块：资产卡片、资产类别、折旧码、原值、累计折旧、历史折旧。
- SAP 总账、成本中心、利润中心：折旧费用科目、费用归属、组织核算维度。
- 项目/在建工程/投资计划系统：未来项目何时转固、转固金额、形成什么资产。
- 预算系统：预算版本、CAPEX 计划、预计新增、预计减少、预计减值。
- Excel 台账：人工调整、部门填报、预算模板、历史测算表。
- 制度文档和配置说明：折旧政策、折旧码含义、预算口径、例外规则。

这也是 ontology 可能有价值的根本原因：问题不是“公式怎么写”，而是“这些跨系统对象在业务上到底如何关联、适用什么规则、结果如何解释”。

## 3. 核心判断

### 3.1 折旧预测不是单纯公式问题

单资产折旧计算是确定性计算，适合由规则引擎、Python/Java 计算服务、ERP 标准逻辑或 Excel 公式完成。难点在于：

- 这项资产到底适用哪条折旧政策。
- 本次预测使用财务口径、税务口径、管理预算口径还是成本口径。
- 新增、转固、减少、报废、减值等事件从哪个期间开始影响折旧。
- 未来资产还没有资产编号时，如何映射到资产类别和折旧政策。
- 汇总结果出现波动时，如何解释原因。
- 数据算得出来但业务上是否合理，例如电子设备用了房屋建筑物折旧码。

### 3.2 Ontology 的价值不是直接算金额

Ontology 在这里的作用不是直接推理出每月折旧金额，而是回答：

- 资产是什么类型。
- 资产类别之间是什么层级关系。
- 哪类资产适用哪条折旧政策。
- 哪个事件影响哪项资产、哪个期间、哪个折旧基础。
- 哪些资产或预算假设违反了企业规则。
- 某个预测结果可以追溯到哪些资产、事件、政策和数据来源。

计算引擎回答“数是多少”。Ontology 和规则层回答“为什么用这套规则算、这个数由什么导致、哪里可能不合理”。

### 3.3 明细先行，再汇总和问答

资产折旧预测应先生成资产级、月份级明细，再做汇总。

```text
每个当前资产 / 未来预计资产
  -> 未来 5 年每个月折旧值
  -> 按公司、部门、成本中心、资产类别、预算版本汇总
  -> 基于明细、汇总、事件链、规则链做解释和问答
```

如果只有汇总结果，系统只能回答“多少”，很难回答“为什么”。因此，底层必须保留“资产 x 月份”的预测明细，以及每条明细关联的政策、事件和计算规则。

## 4. 目标能力

项目目标能力可以分为六类。

### 4.1 自动匹配折旧政策

系统应能基于资产类别、公司、口径、用途、预算版本等信息，自动判断适用的折旧政策。

示例：

```text
注塑设备 -> 机器设备 -> 生产设备 -> 固定资产
机器设备 + 中国大陆公司 + 预算口径 -> 机器设备预算折旧政策
机器设备预算折旧政策 -> 直线法、10 年、残值率 5%、投产次月开始
```

### 4.2 资产级未来折旧预测

系统应对当前资产和未来预计资产分别生成未来 5 年按月折旧明细。当前资产来自固定资产卡片，未来预计资产来自预算新增、CAPEX、项目转固计划或 Excel 假设。

### 4.3 多维聚合

预测明细应支持按以下维度聚合：

- 月份、年度。
- 公司、部门、成本中心、利润中心。
- 资产类别。
- 预算版本。
- 当前资产/未来预计资产。
- 新增、减少、减值、调拨等事件类型。
- 折旧政策或折旧码。

### 4.4 折旧波动解释

系统应能回答：

- 为什么某部门明年折旧增加。
- 哪些资产或预算事件贡献最大。
- 新增、减少、减值、政策变化分别贡献多少。
- 该结果使用了什么折旧政策和开始/停止规则。

### 4.5 What-if 假设分析

假设性问题本质上是新预测场景。系统不应直接在汇总数上加减，而应：

```text
修改某些资产 / 事件 / 规则假设
-> 定位受影响对象
-> 重算受影响资产的月度明细
-> 重新汇总
-> 与基准场景对比
-> 解释差异原因
```

### 4.6 异常识别

系统应识别但不限于以下异常：

- 固定资产缺少原值、启用日期、资产类别、折旧码。
- 资产类别和折旧码不匹配。
- 使用年限超出企业政策范围。
- 残值率小于 0 或超过政策上限。
- 预计新增资产缺少预计投产日期。
- 预计减少资产没有关联存量资产。
- 已报废或已减少资产仍产生未来折旧。
- 资产已到折旧期满但仍在计提。

## 5. 逻辑架构

整体逻辑架构如下：

```text
用户层
  |
  |-- 财务用户
  |-- 预算用户
  |-- 资产管理用户
  |-- 管理层 / 审核人
  |
  v
应用层
  |
  |-- 折旧预测工作台
  |-- 预测结果查询
  |-- 差异解释问答
  |-- What-if 假设分析
  |-- 异常资产清单
  |-- 规则和口径追溯
  |
  v
LLM Harness 层
  |
  |-- 意图识别
  |-- 参数抽取
  |-- 工具编排
  |-- 权限控制
  |-- 输出约束
  |-- 审计日志
  |
  v
业务能力层
  |
  |-- Ontology / 知识图谱
  |-- SPARQL 查询服务
  |-- SHACL 规则校验
  |-- 折旧计算引擎
  |-- 汇总聚合服务
  |-- 差异归因服务
  |-- What-if 场景服务
  |-- 可选 RAG 文档检索
  |
  v
数据层
  |
  |-- SAP 固定资产
  |-- SAP 总账 / 成本中心 / 利润中心
  |-- 项目 / 在建工程 / 投资计划
  |-- 预算系统
  |-- Excel 台账
  |-- 制度文档 / 配置说明 / 报表模板
```

### 5.1 数据层

数据层提供真实企业输入，包括结构化数据、半结构化模板和非结构化制度文档。关键要求是保留来源和版本，不能只保留清洗后的结果。

### 5.2 语义层

语义层使用 ontology / RDF / OWL 表达资产、资产类别、折旧政策、折旧码、事件、口径、组织、预算版本之间的关系，并通过 SPARQL 查询这些关系。

核心类可以包括：

- FixedAsset：固定资产。
- PlannedAsset：未来预计资产。
- AssetCategory：资产类别。
- DepreciationPolicy：折旧政策。
- DepreciationCode：折旧码。
- DepreciationMethod：折旧方法。
- AssetEvent：资产事件。
- BudgetScenario：预算场景。
- AccountingPerspective：核算口径。
- ForecastPeriod：预测期间。
- DepreciationForecastLine：折旧预测明细。
- DepreciationAnomaly：折旧异常。

### 5.3 校验层

校验层使用 SHACL 和业务规则做确定性校验。它不是大模型判断，而是稳定、可重复、可审计的数据质量和业务规则检查。

典型用途：

```text
所有 FixedAsset 必须有原值、启用日期、资产类别和折旧码。
电子设备的折旧年限必须在企业政策允许范围内。
折旧码必须属于该资产类别允许的折旧码集合。
预计新增资产必须有预计投产日期和预算版本。
```

### 5.4 计算层

计算层负责确定性金额测算，不交给大模型。输入包括原值、残值率、使用年限、折旧方法、开始/停止期间、资产变动事件和预测期间。输出是资产级月份明细。

### 5.5 分析层

分析层负责汇总、差异归因和 What-if 场景管理。它基于明细表进行聚合，并保留从汇总结果穿透到资产、事件、政策的能力。

### 5.6 LLM Harness 层

大模型必须通过 Harness 工作，不能直接访问底层数据或自由生成结论。Harness 负责：

- 识别用户意图。
- 抽取部门、期间、预算版本、资产类别等参数。
- 调用 ontology 查询、校验服务、计算服务、汇总服务和差异归因服务。
- 控制输出格式。
- 生成面向业务用户的解释文本。
- 记录审计日志。

### 5.7 RAG 层

RAG 是可选增强，不是一期核心依赖。它适合查询制度原文、配置说明、操作手册和预算口径文档，不能作为规则执行层。

正确职责分工是：

```text
RAG 查依据
Ontology 管语义
SHACL 做校验
计算引擎算金额
LLM 负责解释
```

## 6. 数据与客户前置问题

客户提供的数据越贴近其真实系统和流程，ontology 越不容易变成凭空设计。当前阶段不应要求客户一次性准备完整数据包，但应要求其提供足够的文档、截图、样例和流程说明。

### 6.1 必要数据清单

建议客户优先提供：

- 固定资产卡片表。
- 折旧码/折旧政策配置。
- 资产类别配置。
- 最近 12-24 个月历史折旧明细。
- 固定资产余额表。
- 资产变动流水，包括新增、转固、减少、报废、减值、调拨、停用等。
- 预算新增、预算减少、预算减值假设。
- 项目转固或在建工程计划。
- 折旧预测或年度预算报表样例。
- 固定资产管理制度、折旧政策文件、预算编制口径说明。
- SAP 配置截图或操作手册。
- 字段说明或数据字典。

### 6.2 已有前置问题的判断

当前已有的《资产折旧预测本体 POC 前置问题》方向是正确的。它没有直接要求客户整理完整数据包，而是先问：

- 是否有描述折旧逻辑的现成资料。
- 以前年度预算或折旧预测怎么做。
- 过程涉及哪些系统。
- 是否可以提供少量样例。
- 本次沟通希望先达成哪些理解。

这些问题是必要的，因为 ontology 必须从客户现有规则、流程、系统和样例中抽取，而不是由我们先验设计。

### 6.3 建议补充问题

建议在客户沟通中补充以下问题：

- 是否存在多套折旧口径，例如财务账、税务账、管理预算、成本归集、集团合并口径。
- 哪些字段或规则目前最依赖人工判断，例如折旧码选择、转固时间、停用处理、减值判断。
- 哪些异常需要系统解释，例如折旧额突增、折旧码错配、剩余年限异常、资产已减少但仍计提。
- POC 是否优先选择某一类资产、某一家公司代码、某个部门或某个成本中心。
- 预算预测是否需要与 SAP 实际折旧结果对齐，还是允许存在管理口径差异。

## 7. Ontology 具体作用示例

下面用一个预算新增资产例子说明 ontology 的技术价值。

预算系统中有一条假设：

```text
公司：A 公司
部门：制造一部
资产类别：注塑设备
预计新增金额：1,000 万
预计投产日期：2027-07-15
用途：生产用
预算版本：2027 年年度预算
```

传统 Excel 做法通常依赖人工判断：

```text
人工判断它是机器设备
人工查折旧年限和残值率
人工选择折旧码
人工决定从几月开始折旧
人工套公式
人工解释为什么 2027 年折旧增加
```

有 ontology 后，系统可以表达：

```text
注塑设备 是 机器设备 的子类
机器设备 是 生产设备 的子类
A 公司 属于 中国大陆公司
中国大陆公司 + 机器设备 + 预算口径 适用 P_MACHINE_CN_BUDGET
P_MACHINE_CN_BUDGET 使用直线法、10 年、残值率 5%、投产次月开始折旧
```

推理过程：

1. 输入资产类别为注塑设备。
2. 本体推理出注塑设备属于机器设备，机器设备属于生产设备。
3. 根据公司、资产类别和预算口径，匹配折旧政策 P_MACHINE_CN_BUDGET。
4. 根据政策得出折旧方法、使用年限、残值率和开始规则。
5. 计算引擎计算月折旧额。
6. 系统生成解释链。

计算引擎的输入变为：

```text
原值 = 1,000 万
残值率 = 5%
使用年限 = 10 年
折旧方法 = 直线法
折旧开始期间 = 2027-08
```

计算结果：

```text
可折旧金额 = 1,000 万 * (1 - 5%) = 950 万
月折旧额 = 950 万 / 120 = 7.9167 万
```

系统解释：

```text
2027 年 8 月开始新增折旧 7.9167 万/月。
原因是 2027 年预算包含一项注塑设备新增事件；
注塑设备属于机器设备；
机器设备在 A 公司预算口径下适用 P_MACHINE_CN_BUDGET；
该政策规定使用年限 10 年、残值率 5%、投产次月开始折旧。
```

另一个异常识别例子：

```text
资产名称：办公电脑
资产类别：电子设备
折旧码：房屋建筑物 20 年折旧码
```

公式仍然能算，但业务上可能明显错误。本体和 SHACL 可以识别：

```text
办公电脑 属于 电子设备
电子设备 与 房屋建筑物 是不同资产类别
房屋建筑物折旧码只允许用于房屋建筑物
该资产违反资产类别与折旧码匹配规则
```

这就是 ontology 和规则校验的价值：发现“算得出来但业务上不合理”的问题。

## 8. What-if 假设分析逻辑

假设性问题应被建模为新预测场景，而不是直接修改基准汇总数。

示例问题：

```text
如果 2027 年 7 月新增的注塑设备从 1,000 万改成 1,500 万，未来 5 年折旧会增加多少？
```

执行逻辑：

1. 找到基准场景中的预算新增事件。
2. 复制基准场景，生成 What-if 场景。
3. 将新增金额从 1,000 万改为 1,500 万。
4. 重新计算该预计资产未来 60 个月折旧明细。
5. 更新部门、公司、年度等汇总。
6. 与基准场景做差异比较。
7. 生成原因解释。

如果假设只影响某几个资产或事件，应局部重算。比如新增金额变化、转固日期延后、某项资产提前减少。

如果假设改变的是全局政策，应筛选所有受影响对象后批量重算。比如机器设备使用年限从 10 年改为 8 年、残值率从 5% 改为 3%、新增资产从次月计提改为当月计提。

Ontology 在 What-if 中负责判断：

- 假设改变的是资产、事件还是折旧政策。
- 哪些资产属于受影响资产类别。
- 哪些资产适用受影响政策。
- 哪些预测明细需要失效和重算。
- 差异应归因到哪个假设变化。

## 9. POC 建议范围

POC 不应一开始覆盖全集团、全资产、全口径。建议范围收敛为：

- 一个公司代码。
- 一类或少数几类资产，优先机器设备。
- 一个主要口径，优先预算口径。
- 三类资产事件：新增、减少、减值。
- 一个预测周期：未来 5 年，按月生成明细。
- 少量真实或脱敏样例资产。

POC 演示场景建议为：

1. 政策匹配：输入预算新增资产，自动推理资产类别、适用折旧政策、年限、残值率和开始期间。
2. 折旧解释：解释某部门未来年度折旧增加或减少的原因，穿透到资产、事件和规则。
3. 异常识别：识别折旧码错配、缺少启用日期、残值率异常、预计新增缺少投产日期等问题。

POC 的成功标准不是“覆盖所有折旧情况”，而是证明：

- 能从客户现有资料中抽取并建模真实规则。
- 能解释某个资产为什么适用某条规则。
- 能生成资产级月度预测明细。
- 能从汇总结果穿透到事件和规则。
- 能发现公式本身无法发现的业务异常。

## 10. 项目实施路径

### Phase 0：客户材料收集和业务访谈

目标是确认真实规则、系统链路和 POC 范围。输入包括制度文档、SAP 配置截图、样例报表、历史折旧样例、预算模板和业务访谈记录。

输出：

- POC 资产范围。
- POC 预测口径。
- POC 事件范围。
- 客户数据清单。
- 规则来源清单。

### Phase 1：最小 ontology 和样例数据建模

目标是构建最小可用语义模型。先建资产、资产类别、折旧政策、折旧码、事件、预算场景、组织、期间等核心对象。

输出：

- Ontology 草案。
- RDF 样例数据。
- SPARQL 查询样例。
- 规则来源与 ontology 对象的映射。

### Phase 2：SHACL 校验与折旧计算明细生成

目标是让系统能在计算前检查数据，并生成资产级月度预测明细。

输出：

- SHACL 校验规则草案。
- 异常清单。
- 折旧计算服务。
- 资产 x 月份预测明细。

### Phase 3：汇总、差异归因和 What-if

目标是从明细生成业务可用的汇总结果，并支持场景比较。

输出：

- 按公司、部门、成本中心、资产类别、年度/月度的汇总表。
- 差异归因结果。
- 基准场景和 What-if 场景对比。

### Phase 4：LLM Harness 问答解释

目标是将自然语言问题转为受控查询和工具调用。

输出：

- 问答意图分类。
- 参数抽取。
- 工具调用链。
- 可追溯解释文本。
- 审计日志。

### Phase 5：POC 演示和客户反馈

目标是用有限范围证明价值，并收集客户对规则、口径、异常和解释效果的反馈。

输出：

- POC 演示脚本。
- 关键问答案例。
- 客户反馈清单。
- 下一阶段范围建议。

## 11. 主要风险

### 11.1 客户无法提供真实规则和配置

如果客户只能提供抽象描述，无法提供 SAP 配置、制度、样例报表或历史折旧结果，ontology 很容易变成我们凭空设计的模型，无法证明贴合企业现状。

应对方式：先要求少量但有代表性的配置截图、样例资产、历史折旧明细和预算模板。

### 11.2 SAP 标准配置与预算 Excel 口径不一致

企业预算折旧可能不完全等于 SAP 实际折旧。有些预算假设来自人工 Excel，有些管理口径可能与财务账不同。

应对方式：必须先确认 POC 做哪个口径，并显式建模 AccountingPerspective。

### 11.3 项目价值被误解为简单计算器

如果客户只要求“给定资产和折旧码，算未来折旧”，ontology 的价值很低，Excel 或 ERP 标准功能可能已经足够。

应对方式：POC 必须展示规则匹配、异常识别、结果解释、What-if 影响范围定位等能力。

### 11.4 Ontology 范围过大

如果一开始试图覆盖所有财务场景、全部资产类别、全部核算口径，项目会变成财务百科，难以落地。

应对方式：POC 严格限定公司、资产类别、口径和事件范围。

### 11.5 LLM 被放到错误位置

大模型不能直接计算折旧、修改财务数据或凭空判断折旧政策。否则结果不可审计、不可重复。

应对方式：使用 LLM Harness，所有结论必须来自受控工具、规则、图谱或计算服务。

### 11.6 RAG 被误用为规则执行层

RAG 可以查制度依据，但不能每次从 PDF 临时读规则并让大模型决定如何计算。

应对方式：制度原文应抽取为结构化规则和 ontology 关系，RAG 只用于来源追溯和原文问答。

### 11.7 缺少样例导致无法验证

没有样例资产、样例事件和历史预测结果，就无法判断本体推理和计算是否符合企业实际。

应对方式：客户前置问题必须明确要求少量脱敏样例。

## 12. 待确认事项

在正式做 POC 前，需要与客户确认：

- POC 做哪个核算口径：财务账、税务账、管理预算、成本口径还是集团口径。
- 是否存在多套折旧口径，以及各口径是否需要对比。
- 是否以 SAP 实际折旧结果作为校验基准。
- 未来资产主要来自预算系统、项目系统、投资计划还是 Excel。
- 预计新增资产是否已有明确资产类别、投产日期、所属部门和预算版本。
- 客户最关心的是预测效率、解释能力、异常识别，还是多场景假设分析。
- 是否需要在用户界面展示制度依据或配置来源。
- 哪些异常属于阻断性错误，哪些只是提示或警告。
- POC 选择哪个公司代码、资产类别和部门范围。
- 客户是否允许提供脱敏资产卡片、折旧码配置、历史折旧和预算样例。

## 13. 当前推荐方案

当前推荐方案如下：

```text
使用 ontology 管语义和关系；
使用 SHACL 做确定性校验；
使用计算引擎做资产级月度折旧预测；
使用汇总/归因服务做报表和差异分析；
使用 LLM Harness 做受控问答和解释；
RAG 作为可选增强，不作为一期核心依赖。
```

一期 POC 应优先做“最小可信核心”，而不是大而全平台。建议实现顺序：

1. 整理客户样例数据和规则来源。
2. 建立最小 ontology：资产、类别、政策、折旧码、事件、口径、预算场景。
3. 加载少量样例资产、预算新增/减少/减值事件。
4. 用 SPARQL 查询资产与政策关系。
5. 用 SHACL 识别缺字段和错配。
6. 用计算引擎生成未来 5 年资产级月度预测明细。
7. 汇总到部门、公司、资产类别和年度。
8. 用 LLM Harness 回答“为什么变化”和“如果假设改变会怎样”。

## 14. 项目产物清单

后续项目应形成以下产物：

- 本项目记录文档。
- 客户前置问题清单。
- POC 样例数据包。
- Ontology 草案，包括类、关系、事件、口径、政策。
- SHACL 校验规则草案。
- 折旧预测明细表结构。
- 汇总表结构。
- What-if 场景结构。
- SPARQL 查询样例。
- 计算服务接口说明。
- LLM Harness 工具调用流程。
- 问答/解释场景清单。
- POC 演示脚本。

## 15. 建议的折旧预测明细结构

建议折旧预测明细采用“资产 x 月份”的粒度。

核心字段：

```text
scenario_id
budget_version
asset_id
planned_asset_id
asset_source_type
company
department
cost_center
profit_center
asset_category
depreciation_code
depreciation_policy
depreciation_method
period
opening_original_cost
opening_accumulated_depreciation
opening_accumulated_impairment
opening_net_value
addition_amount
disposal_amount
impairment_amount
depreciable_base
monthly_depreciation
accumulated_depreciation
closing_net_value
source_event_id
calculation_rule_id
validation_status
```

这张明细表是后续汇总、解释、What-if 对比和异常追溯的基础。

## 16. 建议的汇总结构

汇总表可以从预测明细派生，不建议手工维护。

典型汇总维度：

```text
scenario_id
budget_version
period
year
company
department
cost_center
profit_center
asset_category
asset_source_type
event_type
depreciation_policy
```

典型指标：

```text
monthly_depreciation_sum
yearly_depreciation_sum
addition_depreciation_impact
disposal_depreciation_impact
impairment_depreciation_impact
transfer_depreciation_impact
policy_change_impact
```

## 17. 内部共识

本项目最重要的内部共识是：

```text
资产折旧预测 = 资产存量 + 折旧政策 + 生命周期事件 + 预算假设 + 核算口径 + 时间期间。
```

Ontology 的作用是把这些对象、关系、口径和事件结构化，让系统能够稳定回答：

```text
这项资产是什么？
它适用什么折旧政策？
为什么适用？
未来发生了什么事件？
事件从什么时候影响折旧？
预测结果是多少？
这个结果为什么变化？
是否存在异常？
异常依据是什么？
```

如果客户只需要一张固定格式的折旧预测表，Excel 或 ERP 标准功能可能已经足够。如果客户需要跨系统、跨口径、可解释、可审计、可假设分析的折旧预测能力，ontology 才有明确价值。
